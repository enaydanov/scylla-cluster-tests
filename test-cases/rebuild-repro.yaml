test_duration: 120

prepare_write_cmd:  ["cassandra-stress write n=20971520 -schema 'replication(strategy=NetworkTopologyStrategy,us-east1=3,us-west1=0) compaction(strategy=TimeWindowCompactionStrategy,compaction_window_size=18,compaction_window_unit=DAYS)' -mode cql3 native -rate threads=80 -pop seq=1..20971520 -col 'n=FIXED(10) size=FIXED(512)' -log interval=5",
                    ]

n_db_nodes: '3 3'
n_loaders: '1'

simulated_racks: 0

nemesis_class_name: 'NoOpMonkey'

user_prefix: 'rebuild-repro'

append_scylla_yaml:
  rf_rack_valid_keyspaces: false
  allowed_repair_based_node_ops: ""
